export enum UserRole {
  USER = 'user',
  ADMIN = 'admin',
}

export enum OtpType {
  FORGOT_PASSWORD = 'forgot_password',
}

export enum OrderStatus {
  PENDING = 'PENDING',
  PAID = 'PAID',
  FAILED = 'FAILED',
}
