export enum UserRole {
  USER = 'user',
  ADMIN = 'admin',
}

export enum OtpType {
  FORGOT_PASSWORD = 'forgot_password',
}

export enum Status {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  SHIPPED = 'shipped',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
  FAILED = 'failed',
}
